#pragma once
/* Copy this to `wifi_creds.h` (gitignored) and fill in the venue Wi-Fi the
 * hub's STA leg should join for its internet uplink. */
#define STA_SSID "your-venue-wifi"
#define STA_PASS "your-venue-password"
